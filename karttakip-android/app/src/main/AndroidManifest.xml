<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bildirimler (Android 13+ calisma zamani izni ister) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Tam-zamanli alarm: son odeme/kesim hatirlatmalari Doze/uyku modunda bile
         dakikasinda calsin. USE_EXACT_ALARM otomatik verilir. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Telefon yeniden baslayinca alarmlari yeniden kur -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Ekrani uyandiran tam-ekran uyari (alarm gibi) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Pil optimizasyonundan muafiyet iste (Samsung/Doze bildirimi geciktirmesin) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".KartTakipApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KartTakip">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KartTakip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Alarm tetiklendiginde bildirimi gosteren alici -->
        <receiver
            android:name=".notif.ReminderReceiver"
            android:exported="false" />

        <!-- Yeniden baslatmada alarmlari yeniden planlayan alici -->
        <receiver
            android:name=".notif.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Ana ekran widget'i: uygulamayi acmadan ozet gosterir -->
        <receiver
            android:name=".widget.KartWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
    </application>
</manifest>
